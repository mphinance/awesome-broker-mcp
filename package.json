{
  "name": "awesome-broker-mcp",
  "version": "1.0.0",
  "private": true,
  "description": "A verified directory of broker MCP servers — which brokers an AI can actually trade through.",
  "scripts": {
    "check": "node scripts/check-freshness.mjs",
    "check:strict": "node scripts/check-freshness.mjs --strict",
    "lint": "npx awesome-lint"
  },
  "license": "CC0-1.0"
}
